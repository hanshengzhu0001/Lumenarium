# Lumenarium 项目答辩与面试材料

**用途**：毕业设计答辩、研究生面试、技术面试的自述与问答准备。
**日期**：2026-08-20（S4 优化线收口版）

本文档不复述实现细节（那在 `V5_FINAL_ARCHITECTURE_AND_REPRODUCTION.md`），
而是回答三类问题：**你做了什么、为什么这样做、遇到问题怎么解决的。**

---

## 1. 一分钟版本（电梯陈述）

> 我做的是**单张图像到可编辑 3D 场景布局**的生成系统。输入一张室内图，输出带资产、
> 位姿、支撑关系的完整场景，可以直接在 Blender 里编辑。
>
> 我的核心贡献是把最后的布局精修阶段从 **5000 步随机模拟退火**换成了
> **二阶关系条件化 Levenberg–Marquardt 求解器**，配一套**可执行证书系统**：
> 求解器只负责提议，每个分量必须有物理见证、且不劣于现任解，才允许提交，否则按
> 所有权作用域回滚。
>
> 结果是这个阶段快了 **3.5 倍**（677.8s → 192.9s/场景），物理可实现性从
> **54.58% 提升到 62.10%**，同时上游位姿指标基本不动——也就是说提升来自物理修复，
> 不是靠改变位姿刷分。
>
> 最有意思的发现是一个**负面结果**：给求解器更多迭代预算会让分数**变差**。我花了
> 十几轮实验证明这不是调参失败，而是评测指标本身的结构问题——四个评分族里三个
> 锚定在初始化上，所以「优化得更狠」在数学上就是负期望。

---

## 2. 整体设计思路

### 2.1 问题的本质困难

单张图像**欠定**3D 场景。需要同时推断：物体身份、资产标准坐标系、深度、尺度、
支撑父物体、附着面、无碰撞位置。而且**这些误差是耦合的**：

- 检索到的网格如果原点或长宽比不同，最能解释图像的位姿就变了
- 支撑父物体判错，一个局部看起来合理的变换会变成物理上不可能

所以不能把它当成七个独立的回归任务，必须让几何、语义、物理约束在同一个优化问题里
互相制约。

### 2.2 我的两阶段路线

```
V1（原始 Imaginarium）
  └─ V3：支撑感知重建（我的第一阶段贡献）
      └─ V4：接入 DeepSearch 检索（Calvin Gu 与导师在腾讯的团队贡献）
          └─ V5：SceneLM 二阶求解器 + SceneProof 证书（我的第二阶段贡献）
              ├─ V5-fast   论文定量主版本
              ├─ V5-medium +true-mesh 视觉修复，演示用
              └─ V5-best   三冷启 + 无 GT selector
```

**关于归属（面试时务必主动讲清）**：V4 的 DeepSearch 检索由 **Calvin Gu 与导师在
腾讯的团队**贡献。它索引**十万量级资产、单次查询亚秒内**返回最相关候选，使
"逐物体穷举检索"在交互式管线里变得可负担。**这部分在我的模块上游**，我在论文正文、
致谢和 README 里都单独归属。我自己的贡献是 V3 的支撑感知重建与 V5 的
SceneLM + SceneProof 后端。

**第一阶段（V3）解决"支撑关系判错"**：先用 2D 水平重叠 + 3D 竖直接触邻近性
发现候选栈结构，再用保守语义门决定是否真的改父物体。
关键设计决策：**候选发现与改写权限分离**。检测器故意做得比改写宽松——它可以大胆
提候选，但只有"小件货物放在支撑类物体上"这种明确情形才被允许改写支撑树。
理由是 2D 重叠噪声大，无限制改写会用 recovery 换来错误的支撑边。

结果：Primary recovery 从 89.49% → **91.40%**（当前最高）。

**第二阶段（V5）解决"精修慢且不可审计"**：把 5000 步 SA 换成关系条件化 LM。

### 2.3 为什么是二阶而不是一阶

不是因为二阶收敛快（在这个问题上步数少到看不出差别），而是**鲁棒性**：

一阶 Adam 路径用**硬投影**处理支撑约束。在 official_01 这种 112 物体、14 个支撑
足迹初始误差超 0.5m 的场景，半空间约束**没有可行点**，直接抛异常、整个场景失败。

二阶路径把不可行关系作为**加权残差**进入优化，必要时用有界的 relation-release
机制释放，所以是优雅降级。实测 5/5 场景完成 vs 一阶跑不完。

**这个论点值得强调**：一阶在单场景上分数更高（0.6822 vs 0.6338），但对内容生产
管线来说，一个场景崩掉就是让美术师白等一轮，**完成率压倒零点几个分数点**。

---

## 3. 三个核心技术创新

### 3.1 Typed Relation Program（类型化关系程序）

**问题**：优化 loss 低 ≠ 物理上真的成立。传统做法里两者混在一起，无法区分
"我优化得好"和"我真的满足了约束"。

**方案**：把每个场景编译成确定性的关系程序 bundle。每个程序声明四件事：

1. 参与的对象/部件**见证**
2. 拥有的**变量块**
3. 给求解器的**鲁棒残差通道**
4. 用于认证的**独立测量与阈值**

**关键的类型规则**：任何求解器变量的 owner 必须是该 factor 的参与者之一。

这条规则看起来平凡，但它使一件事成为可能：**Jacobian 所有权的数值审计**——
未声明的 Jacobian 块必须在容差内为零。这等于给"这个约束到底动了谁"提供了机器可
验证的答案。

**求解器残差与证书测量刻意分离**，防止低 loss 冒充物理见证。

### 3.2 SceneLM：关系条件化的 SO(3) 守卫 LM

**问题**：直接在 SE(3) 上做 LM，父物体旋转会错误地传递给子物体朝向。

**方案**：每个非刚体对象拥有**独立的世界系旋转切空间** `ω_i`，平移用**关系决定的
chart**（world xyz / support uvh / plane uvn）。子物体中心解码为：

```
t_i = t_π(i) + ΔR_π(i)·(t̄_i − t̄_π(i)) + ΔR_π(i)·B_i·q_i
```

这样**父物体运动会输运子物体中心，但子物体朝向保持独立**。物理直觉是：你推动桌子，
桌上的杯子跟着走，但杯子不会因为桌子转了就自己转向。

求解 `(JᵀJ + λD)Δx = −Jᵀr`，matrix-free + 预条件 CG。接受准则是三重的：
步长有限、非线性目标真的下降、gain ratio 超阈值。

**安全 Schur 消元**：只有稳定叶子支撑块的**平移**可以消去，**旋转永不消元**。
理由是旋转块的消元会破坏独立性假设，而独立性正是上面那个设计的全部价值。

### 3.3 SceneProof：可执行证书与作用域回滚

**问题**：全场景接受/拒绝太粗。一个物体修不好就丢弃整场优化，浪费了其他物体的
正确改进。

**方案**：分量级非劣提交。对每个分量 `c`：

```
m_c(x′) − m_c(x) ≥ −ε_c
```

外加：现任已通过的 hard program 不得变为不通过，不得出现新 hard failure。

失败程序的见证**只标识其变量 owner 与声明的分隔符**，**只回滚这些对象**。
证据缺失给 `Abstain`（而不是判失败），未解决的可见失败必须暴露。

**这个设计改变了"失败"的含义**：不可修复的缺陷从"静默的"变成"被标注的"。
在生产上，调试从肉眼看整个场景变成读一份未解决见证清单。

---

## 4. 遇到的关键问题与解决方案

> 面试时这部分最能体现工程与研究能力。每条都是真实发生的。

### 4.1 研究方法类

**问题 1：从单场景结果外推，两次被推翻**

我看到 2 步档诊断 `rejected=0`，就断言"加预算是纯收益，应该给到 32 步"。
实测 8/16 步发现 `rejected > accepted`、阻尼 λ 涨了 5.7 个数量级、能量只降 4.37%。
后来又在单场景上把 semantic 权重定为默认值，Paper30 全量证明 macro 降了 0.0301。

**解决**：确立执行顺序 **Smoke1 → Smoke5 → Paper30**。改默认值前必须过 5 场景，
改论文数字前必须过 30 场景。**单场景只能用来排除假设，不能用来确立结论。**

**问题 2：断言"某项缺失"而没有先查证**

我曾断言"目标函数缺少水平足迹包含项，这是唯一值得走的路"，并据此规划了代码改动。
真相是该项（`support_planar_containment_loss`）早就存在且权重为 1.0。

**解决**：断言任何"缺失"之前必须三步验证——grep 到函数定义、确认它进了 total
目标、确认权重非零。**这是我从这个项目学到最重要的一课：在没查证的事情上下结论，
比不下结论危害更大。**

**问题 3：拿错基线做对比**

曾拿 legacy SA-5000 当基线，得出"新配置更好"的结论。实际应该跟 Fix61 比，
换了基线之后结论反转。

**解决**：任何实验开始前先明确写下"对照臂是哪个版本名"，并且对照臂与实验臂必须
在同一次评测调用里产出（避免评测器版本漂移）。

### 4.2 工程类

**问题 4：env 变量名落到脚本不看的名字上，静默产生假结果（最危险）**

两个运行脚本的变量名完全不同。我用底层 runner 的变量名去跑全栈脚本，
它用自己的默认值（2 步 + 自己的版本名）跑了一遍基线，**121 秒完成、数字与基线
逐位相同**，差一点被当成实验结果采信。

**解决**：
- 每次跑完必须核 banner 的 `TARGET_VERSION` 与 `ITERATIONS` 两行
- 建立**三重验证**：耗时是否合理、文件 mtime 是否是刚才、
  落盘的 `executed_iterations` 是否等于设定值

**问题 5：worker 幂等跳过导致复用旧结果**

目标版本目录已存在时 worker 会跳过场景，表现为耗时异常短。
**解决**：同上三重验证。这个坑和问题 4 表现相同（耗时短 + 数字似曾相识），
所以验证脚本要同时覆盖两者。

**问题 6：查诊断查错了文件，误判求解器类型**

`scenelm_solver` 诊断只写在 `*_placement_info_s4.json`（优化**后**）。
我查了 `*_placement_info_s3.json`（优化**前**的几何快照），得到 `ABSENT`，
差点得出"生产版本一直是一阶"的错误结论。

**解决**：文档里把三种 JSON 的区别写在最前面（§0）。后来用正确文件拿到铁证：
`schema_version=scenelm_relation_manifold_v1`、`solver=v5_scenelm`，
一阶根本不写这个键，故为落盘自证。

**问题 7：patch 锚点不匹配 / 写坏文件**

远程与本地文件格式不同（本地多行、远程单行），patch 锚点匹配失败。
更早还发生过用 `index('"""', 3)` 误命中 shebang 后的开引号，把文件写坏。

**解决**：
- patch 前先 `grep -n` 看远程真实内容，按实际写锚点
- patch 脚本**必须带 `assert n == 1`**，锚点不唯一或为 0 就 abort
- 禁止基于字符串索引的整体重写，只做定点替换

### 4.3 数据类

**问题 8：`scale=[1,1,1]` 不是中性默认值**

看起来是"不缩放"的安全默认值，实际是在**断言"此物恰好就是其资产授权的那么大"**。
资产库会把它翻译成具体米数。当观测框只剩 2–17cm 碎片时，代码落入小物体分支返回
`[1,1,1]`，于是"凭空长出 1.9m 书架"。

**解决**：联表资产库授权尺寸做审计。**这个结论没有授权尺寸表根本说不出来**——
这是联表工作的全部价值，而不是"错配率是多少"这个数字。

**问题 9：`class_en` 是策展粗桶，不是资产身份**

用它判断"资产是否被替换"会误判一批。例如 `wardrobe_0 → a_SM_Wardrobe_01`
（真实授权 0.998/0.544/2.000）却因桶名 `Storage_locker` 被判为替换。

**解决**：资产标识符作第二独立证人，且**刻意不对称——标识符可以开释，永不可定罪**。
理由是标识符命名不规范（有 `b_33`、`d_1000003614815` 这种），
用它定罪会引入新的误判。

---

## 5. 最重要的结果：一个负面结果

### 5.1 现象

给求解器更多迭代预算，评测分数**变差**。而且四条补救路径全部失败：

| 配置 | macro | 说明 |
|---|---:|---|
| **2 步（生产配置）** | **0.6183** | 最优 |
| 4 步 | 0.5782 | 语义族一次性崩塌 |
| 8 步 | 0.5828 | 已饱和 |
| 8 步 + 掐转角步长 | 0.6004 | 只收回 63% |
| 8 步 + 同时掐平移 | 0.5799 | 又跌回去 |
| 8 步 + 加强初始化正则 | 0.5672 | 最差 |

### 5.2 机制（这是答辩时的亮点）

我最初的假设是"欠收敛"或"参数没调好"。实际原因是**评测指标的结构**：

评测 macro = 四族等权平均。但这四族的性质完全不同：

- **semantic**：被"初始化自一致性"门控筛过——初始化本来违反的关系直接**弃权不评分**。
  所以被评分的恰好是初始化本来做对的那批，**任何位移只有下行**
- **support**：锚定在 S3 支撑树上
- **plane**：对求解器能表达的运动**完全不响应**（所有实验臂恒为 0.7081）
- **collision**：**唯一**一个最优解远离初始化的族

所以 `macro ≈ 3 份「保真度」+ 1 份「改进量」`。求解器下降的是自己的目标函数
（碰撞与接触权重高于语义），而评测是四族等权——**每多走一步，都在以不利的汇率
用保真度换改进量。**

**结论**：2 步不是欠收敛，是**把早停当作对初始化的隐式 trust region**。
截断轨迹就是限制位移范数，也就限制了保真度损失。

### 5.3 为什么这个负面结果有价值

1. **它是可证伪的，而且我做了证伪实验**。如果原因只是"每步转太多"，那么掐转角步长
   同时保持总角度预算不变就该恢复分数——没恢复。如果原因是"隐式正则不如显式"，
   那么加强 warm-start 权重就该让大预算成功——更差了。两个反证使这个解释从
   "一个方便的故事"升级为"可操作的解释"。

2. **它是一个可迁移的测量设计教训**：如果指标是通过"与管线自身初始化对比"来构造的，
   或者对"初始化违反的关系"弃权，那它就悄悄把一个**改进任务**变成了**保持任务**。
   这对整个单图布局方向都是隐患。

3. **诚实报告它比只报 2 步的数字更有价值**。否则后来人会重新撞同一面墙。

### 5.4 为什么「更小步长 + 更多步数」这个直觉在这里失效

**这是答辩最可能被追问的点，必须能当场答出来。**

提问者的直觉是对的、而且是经典优化的标准结论：把信赖域半径减半、迭代数加倍，
最差也是中性，通常更好——迭代点更忠实地跟随下降流，同样的总路径长度下应该到达
**更低**的 $E$，对平移和旋转都成立。

**我们恰好做了这个实验**（不是回避）：
- 8 步 × 3.75° 转角上限 = 2 步 × 15°，**总角度预算完全相同**（30°）
- 再加 8 步 × 0.05 m 平移上限 = 2 步 × 0.20 m，**总平移预算也相同**（0.40 m）

结果两个都**低于** 2 步（0.6004 和 0.5799 vs 0.6183）。直觉失效有三个原因，
**关键是没有一个是"求解器有问题"**：

**原因一：这个直觉是关于 $E$ 的，不是关于评测 $M$ 的。**

细步长确实让我更准确地到达 $\arg\min E$。但根据 §5.2 的分解，那个点在 $M$ 下
**比被截断的迭代点更差**。所以「优化保真度」与「目标错配」是**相乘**的：
错配固定时，优化越准，损失越大。

> 一句话版本：**更细的离散化是"把错的问题解得更精确"。**

**原因二：总路径长度相同 ≠ 净位移相同。**

二阶 LM **每步重新线性化**。8 个小步会沿着弯曲的下降路径持续朝一致方向累积；
而 2 步轨迹是被截断的，并且实测**第 2 步就被 gain-ratio 准则拒绝了**
（落盘诊断 `accepted=1, rejected=1`）。

所以在标称上限相同的情况下，budget-8 实际实现了**更大**的有效位移
$\|T-\bar T\|$——**与我原本想做的控制恰好相反**。这是我设计这个对照实验时没想到的，
是实测数据告诉我的。

**原因三：两个步长上限不是两个独立的一维线搜索。**

LM 的步是把平移和旋转**联合求解**的，全局 trust scale 只等比缩放整个步、
不改变方向。所以限制一个坐标，剩余预算会被**重新分配**给另一个。

这在数据里直接可见：
- 只掐转角 → 收回 63% 的语义损失，但 support 掉到 **0.5391，是全部实验臂里最差的**，
  因为释放出的预算被花在平移上，把子物体从父物体台面上推了出去
- 同时掐平移 → 又把 `PointTowards` 和 `Distance` 残差需要的平移自由度拿掉了，
  刚收回的语义分又丢了

**结论：这个二维上限空间里不存在内部甜点**，因为两个上限是通过**同一个信赖域**耦合的，
不可分离。

---

### 5.5 各物理分量的权重设计理由

| 项 | 权重 | 设计理由 |
|---|---:|---|
| contact（竖直间隙） | **2.0** | 硬物理违背。悬空是**无歧义的错**，视觉上极其刺眼 |
| plane（墙/天花板附着） | **2.0** | 同上，脱墙是硬违背 |
| collision | 1.0 | **刻意不调高**：它是唯一的 pairwise 项，残差数随物体数 $O(N^2)$ 增长，权重再高会让密集场景淹没所有 per-object 项 |
| containment（水平足迹包含） | 1.0 | 保证子物体**整个足迹**在父物体内，对齐评测口径而非只测中心点 |
| boundary（房间边界） | 1.0 | 当前基准里场景覆盖退化，仅作诊断 |
| semantic | **0.5** | 关系来自语言模型场景图，**单条不可靠**。过度信任会把物体转到错误朝向 |
| orientation | **0.25** | 只对贴墙物体有意义，且朝向误差在视觉上比穿模温和得多 |
| warm_start | **0.01** | 图像证据**已经编码在初始化 $\bar T$ 里**，这里只需要极弱的锚定防漂移 |

**两条贯穿的设计原则**（答辩可直接引用）：

1. **硬物理违背优先于软偏好**。穿模/悬空是无歧义的错误；25° 的朝向偏差在一张
   有歧义的图上是可辩护的解释。所以 contact/plane 是 2.0，semantic 是 0.5。
2. **噪声证据的权重低于可靠证据**。语言模型给的关系单条不可靠 → 0.5；
   初始化已含图像证据 → 只需 0.01 的锚。

**warm_start 为什么不能调高**（实测反证）：调到 0.3 时**所有族都变差**
（semantic 0.4563 垫底、collision 0.4130 也垫底）。机制是它开始与物理残差
**争夺同一个 Gauss–Newton 步**——不是"锚得更牢"，而是把联合求解压垮了。
这条实测把"显式正则应该优于隐式早停"这个看似合理的假设否掉了。

---

### 5.6 各物理分量对优化的响应（最可迁移的观察）

**四个评测族不是"物理质量"的可互换代理**，它们在「求解器位移能否帮到它」这件事上
性质完全不同：

| 族 | 对位移的响应 | 实测 | 机制 |
|---|---|---|---|
| **collision** | **唯一正响应** | 0.4301 → 0.4436 | 唯一一个最优解**远离**初始化的族（初始化有穿透时） |
| **support** | 负响应 | 0.5684 → 0.5480 | 三分量构成（竖直 gap + 水平 containment + 足迹重叠）。**只有竖直分量有强权重残差守住**，花在平移上的预算通过两个水平分量伤害它 |
| **plane** | **完全无响应** | 恒为 0.7081（所有臂） | 在我们的运动模型下，求解器能表达的运动不改变墙/天花板父物体的水平法向距离与朝向角。**它是纯 S3 属性** |
| **semantic** | **最敏感，崩塌式** | 0.7634 → 0.5209 | 双重不利：① 被 warm-start 自一致性门控筛过，评分子集恰是初始化做对的那批；② **评测取 per-object max 而残差取 mean**，所以"平均改善但每个物体有一条关系恶化"被判为损失 |

**这个表的价值**（答辩时的升华）：

> 报一个 macro 平均分**掩盖了「四分之三的分数其实在测初始化保真度」这个事实**。

对整个单图布局方向的启示：如果你的指标是通过「与管线自身初始化对比」构造的，
或者对「初始化违反的关系」弃权，那它就悄悄把一个**改进任务**变成了**保持任务**。



### 5.7 我明确声明的边界（答辩时主动说）

- 这个结论是**网格有界**的：覆盖了步数、平移/转角步长上限、语义权重、
  warm-start 权重，但不是"所有正则器"
- 它是**相对于这套等权 macro** 成立的。如果指标给碰撞更高权重，或者对初始化违反的
  关系也评分，最优点会移动
- 还有一个**已识别但未修**的错配：求解器对语义残差取**均值**，评测取
  **每物体最大值**。求解器可能"平均改善但最差关系恶化"。这是代码事实，
  但它是否为主因**我没做隔离验证**，所以不声称

---

## 6. 量化结果汇总（答辩必背）

### 6.1 质量（Paper30，Primary = 可见掩码 ≥ 8000px）

| 版本 | Recovery | Parent | Rot AUC@60 | Trans AUC@0.5m | Physical macro |
|---|---:|---:|---:|---:|---:|
| V1 原始 | 89.49% | 89.32% | 48.13% | 23.73% | 52.98% |
| V3 支撑感知 | **91.40%** | 87.80% | 48.11% | 20.36% | 审计中 |
| V4 DeepSearch | 88.22% | 80.14% | 31.34% | 12.19% | 54.58% |
| **V5-fast** | 88.22% | 80.14% | 31.38% | 12.14% | **62.10%** |

### 6.2 速度

| 范围 | 均值/场景 | 加速比 |
|---|---:|---:|
| Legacy SA5000 S4 | 677.77 s | 1.000x |
| **SceneLM/Fix61 S4** | **192.93 s** | **3.513x** |
| +Fix114（V5-medium） | 359.26 s | 1.887x |
| V5-fast 全链路 S0–S4 | 829.88 s（13.83 min） | — |

S0–S3 分解：S0 9.69s / **S1 443.04s（69.56%，最大瓶颈）** / S2 137.45s / S3 44.79s

### 6.3 必须诚实说明的两点

1. **V3→V4 的位姿大幅下降发生在我的模块之前**，是 S2 检索器换成 DeepSearch 导致的
   （不同的标准坐标轴/原点/尺度）。因为 V4 和 V5 共享冻结的上游输出且位姿指标几乎
   相同，**这个下降不能归因于 SceneLM/SceneProof**。但这是归因证据，不是严格因果——   需要 seed 锁定、冻结 S1、只切换 S2 的配对实验。

2. **V3 的物理分数有评测器出处冲突**（41.20% vs 52.14%），已从主表剔除。
   在出处对齐之前公布任何一个都是误导。

---

## 7. 预设答辩/面试问题

**Q：迭代 2 步太少了吧？经典优化里减小步长、增加步数总是更好的，你为什么不这么做？**
A：这个直觉是对的，而且我**做了这个实验**。我把转角上限从 15° 降到 3.75°、步数从
2 提到 8，总角度预算完全相同都是 30°；又把平移上限从 0.20m 降到 0.05m、总平移也
相同都是 0.40m。结果两个都低于 2 步（0.6004 和 0.5799 vs 0.6183）。

三个原因。第一，这个直觉是关于**求解器目标 $E$** 的，不是关于**评测 $M$** 的；
细步长让我更准确地到达 $\arg\min E$，而那个点在 $M$ 下更差——所以细化离散化是
"把错的问题解得更精确"。第二，**总路径长度相同不等于净位移相同**：二阶 LM 每步
重新线性化，8 个小步沿弯曲下降路径持续朝一致方向累积，而 2 步轨迹是被截断的、
且落盘诊断显示第 2 步就被 gain-ratio 拒绝了（`accepted=1, rejected=1`），
所以 8 步的**有效位移反而更大**，与我想做的控制正好相反。第三，两个上限**不是
两个独立的一维线搜索**：LM 联合求解平移和旋转，全局 trust scale 只等比缩放整个步，
所以掐一个坐标会把预算重新分配给另一个——实测只掐转角时 support 掉到全部实验臂
最差的 0.5391，因为释放的预算被花在平移上把子物体推出了台面。这个二维上限空间里
没有内部甜点，因为两个上限通过同一个信赖域耦合。

**Q：你的权重是怎么定的？调参调出来的吗？**
A：不是网格搜索，是两条设计原则推出来的，然后用实测反证检验。原则一，
**硬物理违背优先于软偏好**：contact 和 plane 是 2.0，因为穿模和悬空是无歧义的错、
视觉上极其刺眼；semantic 是 0.5，因为 25° 的朝向偏差在一张有歧义的图上是可辩护的
解释。原则二，**噪声证据权重低于可靠证据**：语义关系来自语言模型场景图、单条不可靠，
所以 0.5；warm_start 只有 0.01，因为图像证据已经编码在初始化里，只需要极弱的锚防漂移。
collision 刻意保持 1.0 而不调高，因为它是唯一的 pairwise 项、残差数随物体数平方增长，
权重再高会让密集场景淹没所有 per-object 项。

反证也做了：warm_start 调到 0.3 时**所有族都变差**，机制是它开始与物理残差争夺
同一个 Gauss–Newton 步。这把"显式正则应该优于隐式早停"这个看似合理的假设否掉了。

**Q：四个物理分量是等价的吗？**
A：不等价，这是我最可迁移的一个观察。它们在「求解器位移能否帮到它」上性质完全不同：
**collision** 是唯一最优解远离初始化的族，所以是唯一随预算改善的；**plane** 在我们
的运动模型下完全不响应（所有实验臂恒为 0.7081），它其实是纯 S3 属性；**support**
是三分量构成的，但只有竖直间隙有强权重残差守住，花在平移上的预算通过两个水平分量
伤害它；**semantic** 双重不利——被 warm-start 自一致性门控筛过，而且评测取 per-object
最大值而残差取均值，所以"平均改善但每个物体有一条关系恶化"被判为损失。

所以报一个 macro 平均分会掩盖「四分之三的分数其实在测初始化保真度」这个事实。

**Q：你的工作和 Imaginarium 的区别是什么？**
A：Imaginarium 提供视觉引导的整体分解（提示词→图像→解析→检索→位姿→精修）和资产库，
我保留这个分解。我的改动集中在两处：支撑推理（V3）和最终精修（V5）。
具体是把 5000 步 SA 换成二阶关系条件化 LM + 证书系统。我在论文和 README 里都明确
区分了继承部分与我的贡献。

**Q：为什么不用学习方法端到端做布局？**
A：三个理由。第一，可编辑性要求输出是显式的资产+位姿+关系，不是隐式表示。
第二，物理约束（无穿透、有支撑、贴墙）是**硬约束**，学习方法只能软性鼓励，
而我的证书系统可以给出机器可验证的见证。第三，数据量不支持——我们是 30 场景基准，
不是百万级数据集。

**Q：3.5 倍加速是怎么来的？是不是只是少迭代了？**
A：不是简单少迭代。SA 的 5000 步是随机提议+Metropolis 接受，每步几乎不带信息；
LM 的每步用一阶导数信息解一个阻尼正规方程，是**证据条件化**的。另外
matrix-free CG + 关系流形降低了每步的变量维度（贴面物体只有 2 个平移自由度）。
诚实地说：迭代数确实只有 2，但这个 2 是**实测最优**而不是为了快而砍的，
我用 4/8/16 步的完整阶梯证明了加预算反而更差。

**Q：物理提升 7.52 个点，会不会是靠改坏位姿换来的？**
A：不会，这是我特意设计的对照。V5-fast 和它的上游 V4-deepsearch **共享冻结的
S0–S3 输出**，位姿指标只变了 +0.04 / −0.05 个点（在噪声内），而物理从 54.58%
涨到 62.10%。所以提升来自物理修复，不是位姿漂移。

**Q：你的证书系统能证明什么？不能证明什么？**
A：**能**证明：每个被接受的编辑都有声明的见证、通过了独立测量的物理/视觉不等式、
且有字节安全的回滚状态。**不能**证明：全局最优性。它不是形式化证明，是可执行的
"无害性"检查。我在论文里明确写了这个界限，不用"proof"这个词误导读者。

**Q：这个负面结果会不会说明你的求解器不行？**
A：恰恰相反，它说明我的**度量**设计得不对。二阶求解器在唯一可改进的族（碰撞）上
比 SA 提升了 15.6% 相对量，而且是唯一能跑完全部场景的（一阶会因硬投影无可行解崩溃）。
分数被压住是因为四族等权里三族奖励"与初始化一致"。这是我主动发现并报告的
**测量设计问题**，不是求解器缺陷。

**Q：如果给你更多时间，下一步做什么？**
A：优先级明确。第一，S1 占 S0–S3 的 69.6%，是全链路最大瓶颈——继续优化精修阶段
收益很小，应该攻 S1 的 API 并发。第二，修求解器 mean 与评测 max 的聚合错配。
第三，做 seed 锁定的 S2-only ablation 把 DeepSearch 的影响从冷启动混杂中分离出来。
第四，完成 V5-best 的无 GT selector。

**Q：项目里你最大的失误是什么？**
A：在没查证的事情上下结论。我曾断言目标函数缺少水平足迹包含项、并说这是"唯一值得
走的路"，实际上该项早就存在且权重为 1.0。类似的还有从单场景外推、拿错基线。
这些让我建立了硬性纪律：断言"缺失"前必须 grep 到定义并确认权重非零；
改默认值前过 5 场景，改论文数字前过 30 场景。**我把这些坑全部写进了复现文档，
就是为了让后来人不必重走。**

---

## 8. 打包内容索引

| 类别 | 文件 |
|---|---|
| **答辩自述** | 本文件 |
| **架构与复现** | `V5_FINAL_ARCHITECTURE_AND_REPRODUCTION.md` |
| **论文** | `paper_draft/sceneproof_paper_draft.pdf` + `.tex` + `README_DATA_PROVENANCE.md` |
| **汇报 PPT** | `Lumenarium_暑期实习总结_完整版_2026-08-19.pptx` + 逐页讲稿 |
| **数学形式化** | `docs/SCENEBA_MATHEMATICAL_FORMULATION.md` |
| **质量速度报告** | `V5_FAST_FINAL_QUALITY_SPEED_REPORT_2026-08-13.md` |
| **实验推理** | `SCENEPROOF_FINAL_EXPERIMENT_REASONING_2026-08-13.md` |
| **评测看板** | `EVAL_DASHBOARD.ascii` |
| **跨版本 4 图对比** | `visual_results/`（livingroom_10 与 official_02 的 input/V1/V3/V5） |
| **Demo 图** | `demo/demo_0..3.png` |
| **Paper30 渲染** | `sceneproof_vertical_support_visual_paper30_fix114_renders.tar.gz` |

---

*本文件与 `V5_FINAL_ARCHITECTURE_AND_REPRODUCTION.md` 配套：前者回答"为什么"，
后者回答"怎么做"。*
