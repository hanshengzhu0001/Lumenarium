# Lumenarium 暑期实习总结（完整版）逐页讲稿

**这份文件由 pptx 导出，pptx 是唯一真源。**
备注栏里就是同一份内容，放映时用演示者视图即可看到，不必另开这个文件。

每页开头的 `[NN/25] Ss` 是页序与秒数；秒数按每秒 4.5 字估出，所以它和内容长度永远自洽。
「只给自己看」以下是动作与口径提示，**不投屏、也不念出来**。

## 01 · SUMMER INTERNSHIP REVIEW · 2026

[01/25] 13s · 封面

大家好。这两个月我做了一件事：把一个2D效果图到3D场景的工作流，变成一个更快、能自己检查对错、可以直接交给美术用的系统。

主要去改进了之前一个单张图生成3D场景的工作流采取的方法包括重构场景检索，显式化建模几何支撑关系提升恢复质量，把无方向的物理探索变成基于梯度的全局优化，做几何证明实现可交付

最终在优化阶段实现了3.5倍的提速

— 只给自己看 —
· 一句话讲完就翻页，不要在封面停留。

## 02 · 场景：技术美术拿到一张参考图

[02/25] 28s · 技术美术的一天

不影响主要表现

先讲人，不讲技术。技术美术要的不是一张好看的效果图，是一个拖进引擎就能开工的场景。

可看着对的场景常常一用就露馅：杯子悬空两厘米，静态图看不出，动画一动就飘；窗帘没找到，整个镜头得重做。

所以这两个月我在回答一个问题：怎么让系统只交出它能证明是对的东西。

— 只给自己看 —
· 三个例子只挑两个说，杯子和窗帘就够。
· 落到右边那句大字上再翻页，这是全场的主线句。

## 03 · 先定义“好场景”：光子真正需要什么？

[03/25] 17s · 什么叫好场景

先说清什么叫好。排序是：该有的不能少、要快、不能穿模、关系要成立。

让步的地方也写明了：朝向差一点可以接受；少数悬空的，宁可明说我证明不了，也不假装它对。

— 只给自己看 —
· 念四条排序，右边让步那栏一句话带过，不展开。

## 04 · 我继承的系统：Imaginarium 把路走通了

[04/25] 41s · 我接手时的系统


说一下我接手时它长什么样。它能跑通，但有两处很贵。

一处是慢，而且慢的地方跟直觉不一样。整条链路的大模型调用其实只有几十次，不算多；问题是被限流卡在有效并发 1，只能一个一个排队等。实测第一步就要七分多钟，占冷启动的七成。

另一处是不稳定：谁放在谁上面，是让大模型用文字回答的。它会把柜顶的箱子写成放在地上，物理一跑，箱子就穿过地板掉下去。

所以最后一步只能用五千步随机搜索兜底：前面每步都不确定，后面只能靠多试碰运气。这就是我从上游改起的原因。

— 只给自己看 —
· 收在「前面不确定、后面只能兜」这句因果上，这是整段的目的。
· 被问调用次数：几十次量级，大部分并行发出。**不要说两万五千次**——那个数字是本地 GPU 上的特征比对，不是网络请求。
· 被问找资产的流程：先用 DINOv2 特征粗筛，再用特征相似度加尺寸差加类别加分精排，两步都在本地；VLM 在这一段只做一件事，就是用原图拼成的 3×3 网格校正物体长宽。
· 被问「那 443 秒到底花在哪」：诚实答我还没拆开逐次耗时，只能确认这一段占七成、且有效并发是 1。
· 被问为什么不做 Flux 微调：论文里它把检索命中率从 48.6% 提到 68.7%，收益是真的；但那是对某一批资产过拟合，而我们要交的是能换资产库的系统。这是主动取舍，不是没做完。

## 05 · 原始系统不是“不能生成”，而是难以稳定交付

[05/25] 14s · 原来的四类毛病

这是当时的输出。能生成，但有四类问题挡在交付前面：资产缺了、太慢、互相穿模、关系不成立。后面所有工作都是冲这四条去的。

## 06 · 两个月的主线：结构 → 速度 → 证明 → 服务

[07/25] 9s · 两个月的主线

这是两个月的主线：V1 到 V3、V4、V5，最后落成一个能上网访问的服务。

检索阶段又慢又容易物理不一致

高成本的语义精排粗筛前移

初步落位，检索耗时显著下降，为后续

把支撑关系显式几何化，用几何约束替代文字判断，提前去掉不可能的支撑组合。这提升了恢复质量

这方便我们
我们把5000随机搜索换成了有方向的梯度收敛

Sceneproof，还加入了几何的可证明性，可被交付
验证优化后资产是否满足几何约束

— 只给自己看 —
· 手指着五个节点扫一遍就行，不要逐版解释，后面的页会讲。

## 07 · 当前 Lumenarium：一张图走完整 S0–S4

[06/25] 65s · 完整链路五步

这是现在的完整链路，五步。我一步步说它拿到什么、交出什么——后面所有的问题和改进都挂在这五步上。

S0 拿一张概念图，交出深度图，也就是每个像素离相机多远。场景的尺度是从这来的。

S1 拿图和深度，交出三样：每个物体的抠图、套在它外面的三维框，还有一张关系图，谁放在谁上面。

S2 拿物体的小图，去资产库里找长得最像的几个模型。

S3 拿这些候选和框，用几何和外观两条线打分，选定一个，再把它对齐回图里物体的位置，交出每个物体的位置、朝向和大小。

S4 拿所有位置加那张关系图，交出一个不穿模的布局、一张渲染图，和一份检查报告。

这五步里，我重做的是后三步：找资产、定位置，和最后的布局优化。

— 只给自己看 —
· 这一页是「带过但要讲清」：五步的输入输出都要说出口，别只指着方框说有五步。
· 被问 S1 细节：SAM3 出抠图，DepthAnything 出深度，抠图里的像素按深度反投影成点云，再拟合出三维框。

## 08 · 先看它做完之后是什么样：三档实机录屏

[08/25] 27s · 三档实机录屏

知道链路长什么样了，现在看它跑完是什么样。同一张输入跑三档模式，这是真实运行的录屏，剪辑过、也快放了。

最重要的一句：三档不是三个模型，是同一条流水线上的三种交付标准。越往右越保守、要求的证据越多；前面的重建结果共用，换档只重跑最后一步。

— 只给自己看 —
· 放视频，少说话，让画面自己走。
· 主动说明剪辑并加速过——承认的成本很低，被人看出来的代价很高。
· 封面那块牌子和底部链接都能点，直接跳 B 站；若已把 mp4 放进仓库根目录，就是原地播放。

## 09 · 那就现在跑一次：同一条链路，现场从头走

[09/25] 28s · 现场跑一次

录屏毕竟剪过，所以现在真跑一次。这是部署在两块显卡上的在线服务：给它一张参考图，拿到一个能直接在 Blender 里打开的场景。

我上传左边这张，模式选 V5，随机种子固定。今天不用缓存，从头完整跑一遍，大概十几分钟。它在后台跑着，我们接着往下讲。

— 只给自己看 —
· 先把图上传、选好档、点 Generate，让进度条动起来，再开口说话。
· 这一页只放输入图。输出此刻还不存在，跑完直接切浏览器看，不要往幻灯片里补图。
· 网络不通就说「刚才那段录屏就是它的完整过程」，往下走，不要在台上 debug。

## 10 · V3 的 support-aware：用几何判据替掉文字判断

[10/25] 58s · 支撑关系怎么算出来

回到刚才那个错误：谁放在谁上面，原来是让大模型用文字回答的，这很不稳。V3 把它换成用包围盒去算，变成纯几何的判断。

怎么算？两个物体，先看它们从上往下投影重叠够不够多，再看竖直方向的缝隙够不够小。两条都满足，就认定是上下关系。阈值都用物体自己的尺寸算出来，没有写死的数字。

接下来这点我想特别说清：高度根本不参加搜索。退火只在水平面上挪东西，竖直方向是算出来的——从地面往上，每个物体的底面贴到下面那个物体的顶面；下面一挪，上面的东西整体跟着挪同样的距离。

放进柜子里的东西更简单，它连水平搜索都不参加，直接跟着柜子走。

— 只给自己看 —
· 这一页是重中之重，不要压。三步各一句，讲完停半秒再翻页。
· 补一句：这条线在 V5 完整保留，今天还在跑，一个开关都没关。
· 被问为什么还信大模型判的高层关系：它错基本只错在落地那一层，高处的父子关系判得对，所以这条链能用。

## 11 · SceneLM 到底在最小化什么？

[11/25] 59s · 把关系写成一张扣分表

当时没有解决软体几何近似，接触可证明性，和物理分量单独门控

刚才那件事只回答了谁在谁上面。判对之后该摆在哪，V3 还是交给随机搜索去碰——它说不出为什么该往这边挪。

所以 V5 换了思路：把这些关系写成一张扣分表，加入梯度让程序去解。这就是 SceneLM。

表上每条都是场景该满足的事：不能互相穿透、该挨着的要真挨上、靠墙的要贴住墙，再加语义偏好，比如椅子该朝着桌子。哪条不满足就扣分，我们要找总扣分最少的摆法。

重点是这些权重怎么读。几何上的硬要求，权重是语义偏好的四倍，意思很直白：好看要让位于成立。而「别离初始位置太远」只给了 0.01，是全场最小的——上游给的初值只是参考，不许它锁死答案。



— 只给自己看 —
· 开头那句转折是全场的枢纽：从「判对关系」到「按关系求解」。
· 收一句：怎么权衡不再藏在谁的调参手感里，而是写在这儿，可以被质疑。
· 方法段从这里开始不可压。先念总式，再讲权重表。

## 12 · 二阶方法：先把不可微的地方修成可微

[12/25] 92s · 为什么敢用二阶方法

这一页回答两件事：我们在解什么，以及为什么不能用最常见的优化方法。先说解什么——上一页那张扣分表，要调的是每个物体的位置和朝向。每条约束都换算成还差多少，插进去三厘米就记 0.03。这些分量一起构成向量r，总扣分就是它们的平方和；我们要让清单上每个数尽量变成零。

那为什么不能直接梯度下降？麻烦出在碰撞。两个物体撞上了，往哪边推最省力？沿 x 推三厘米、沿 y 推 3.1 厘米，那就推 x。可稍微一动，这两个数调了个头，最省力方向瞬间从 x 变成 y。

这就像走在屋脊上：越过脊线，下坡方向整条换掉。优化器朝这一侧走、走过头又跳回来，于是左右横跳。

所以先钉住方向：一旦判定两个物体插在一起，就固定往哪边推，不再变。这条约束就变回一个普通数字——还差几厘米。地形平了，二阶才敢上。顺序不能反。

然后才是屏幕上这个式子。δ 是这一步要挪多少，直接加到位姿上；J 是灵敏度，物体动一点点、差距变多少；λ 是刹车，步子不靠谱就自动收紧。

— 只给自己看 —
· 顺序就是论证：解什么 → 写成清单 → 方向会跳 → 先钉住方向 → 才上二阶。别跳步。
· 被问「斜率怎么跳」：不是正负跳，是最省力的那条轴整条换掉——本来该往左推，一瞬间变成该往前推，两个方向互相垂直。数学上是取两个重叠深度里较小的那个，这个「取较小」不可微，像绝对值函数在零点左右导数不同。
· 被问「120 个数算多吗」：不多，这个规模常规解法也是毫秒级。难点不在数量，在碰撞这一条不可微；而随机搜索在同一个空间里没有方向，只能用一万次迭代乘上每次的两两碰撞检测，去换一个收敛的概率。
· 被问内存：灵敏度矩阵和它的平方从不真的存下来，用两次自动求导直接算乘积，所以内存和最普通的梯度下降一样。

## 13 · 为什么快：只解该解的那部分变量

[13/25] 91s · 为什么快，以及什么时候失灵

接着上一页。式子能解了，这一页说它为什么快、什么时候失灵。快的原因不在二阶，有两条更朴素的。

第一条：关系本身就决定了每个物体要调几个数。放在桌面上的只能在桌面上滑，两个数；贴墙的沿墙面滑，也是两个；完全自由的才三个。而且只有真正违规的物体才进方程，其他的根本不动。

第二条叫 Schur 消元，名字吓人，做的事很朴素。桌子挪五厘米，杯子跟着挪五厘米——杯子的位置完全由桌子决定，没必要当成独立的未知数去解。做法是先把杯子的要求，比如不许掉出桌沿，折算到桌子那一行，再把杯子划掉、只解桌子；桌子解完，一步就能把杯子算回来。折算过了，所以是精确的。

什么时候失灵：两个物体刚从分开变成接触那一瞬间，地形是折的，二阶给的步子不可信。这时我们干脆不求导，只在确实违规的那几个方向上正反各试一小步，留下真变好的那个。慢，但一轮只碰几次。

最后一句口径要说清：三倍半的加速来自刚说的两条，不是来自二阶求解器——它默认还关着，在等验证。

— 只给自己看 —
· 最后那句口径必须说出口，这是防止别人替你夸大的唯一办法。
· 被问「退化会不会拖慢」：不会，接触切换只是一帧，其余都走主路径。
· 被问「凭什么说精确」：因为消元前先把杯子那一行的信息折算进了桌子那一行，不是把杯子的约束扔掉。对当前这次线性化，它是等价变形。
· 准入条件（被追问才说）：只有「有父物体、自己又没有子物体」的才允许被消掉；旋转永不消掉，否则父物体的朝向误差会传给子物体。
· 超时先砍 Schur 那一段，只留「只动违规的物体」。

## 14 · SceneProof：proposal 不等于 commit

[14/25] 36s · 凭什么算数：SceneProof

关系写成了约束，也解出来了，还剩最后一个问题：优化器给的新位置，凭什么就算数？

这就是 SceneProof。每次改动都只当提议，不当结论。先在局部量一遍：该接触的地方是不是真贴上了、有没有新的穿透；再看全局分数有没有变差。两样都过才提交，一样不过就整步退回。

既证明不了、又不能安全退回的，就明确标成未证明，不记成成功。

— 只给自己看 —
· 五个方框不要逐个念，指着流程把三种结局说清：提交、退回、未证明。

## 15 · physical macro 是怎么定义的，为什么它可信

[15/25] 32s · 这个指标为什么可信

指标是这么定的：误差越小分越高，超过容差就归零；总分取平均，同时另报一个取最差值的版本，防止个别严重错误被平均掉。

这页我最想说中间这条。我原本想用墙面反推地板高度，实测发现推出来的地板落在真地板下方两米八到三米一，而正确答案是正两厘米。差得离谱，所以我把这段代码删了，而不是调参数救它。

— 只给自己看 —
· 三条设计只详说墙面反推那条，另两条各一句。这页最能体现研究态度。

## 16 · 质量结果：V3 找回更多，V5 让场景更物理

[16/25] 21s · 质量结果

结果分两块看。找回物体的比例，V3 最好；物理上站得住的程度，V5 最好——它们解决的不是同一个问题。

为什么能确定这 7.5 个百分点来自最后一步？因为 V4 和 V5 前面几步完全一样。

## 17 · 速度结果：全链路从约 23.8 分钟降到 13.83 分钟

[17/25] 25s · 速度结果

整条链路从 23.8 分钟降到 13.83 分钟，最后一步单独快了三倍半。

接上前面的伏笔：当初调接口之所以贵，是限流让它们排队等。现在第一步占冷启动将近七成，并发开到八路估算能省两百秒——这是按容量算的估算，不是实测。

— 只给自己看 —
· 「估算不是实测」这句一定要说，否则会被追问数据来源。
· 被问「十几分钟算快吗」：里面有两种成本，一种随算力线性下降，另一种是算法上的收益、已经落袋了。

## 18 · 同一客厅：从“重建出来”到“整体可用”

[18/25] 7s · 同一客厅的对比

同一张输入，从左到右。最右边是 V5：更完整，而且整体能用。

虽然个体资产的位姿可能有偏差，但整体的空间关系和物理逻辑有

## 19 · 同一办公室：更快检索，同时保持关键资产覆盖

[19/25] 0s · 同一办公室的对比

方法不依赖逐资产标注，而是显式几何

这是办公室场景的同一组对比，结论一样。

— 只给自己看 —
· 默认跳过——第 8 页录屏已经覆盖同样结论；超时就先砍这一页。
· 被追问单个物体的朝向时才翻回来，答：检索更快、覆盖更全，但单体朝向和位置仍需校准。

## 20 · 不止是论文代码：已经变成双 A10 端到端服务

[20/25] 14s · 它已经是个线上服务

这套东西现在不只是论文里的代码：两块显卡、三种模式、缓存和断点续跑都在线上。刚才那次运行走的就是这条链路，我们切过去看一眼。

— 只给自己看 —
· 完整跑通常还没结束，那就展示进度条和阶段灯——它本身就是「真在跑」的证据。
· 跑完了就展示场景，和状态行里那个随机种子。
· 若结果报了未证明，主动说：它给出了结果，同时点名了自己证明不了的关系。宁可这样，也不把没验证的东西记成成功。

## 21 · 我给这两个月打 8 分

[21/25] 31s · 自评 8 分

我给这两个月打 8 分。左边是做到的：整条链路闭环、因果说得清、诚实是设计出来的、结果可复现。

扣掉的一分半说具体些。物体朝向这个指标我没修回来，从 48% 掉到 41%，我定位到它出在哪两步之间，但没能在实习期内修好。二阶求解器代码写完了却默认还关着——架构做完不等于上线。

— 只给自己看 —
· 这一页是主动的自我批评，比任何成绩都更能建立信任。语速放慢。
· 还有一条也要说：有一段时间我只加检查工具、不改主流程，收益明显变小了还没收手，这一点我应该更早察觉。

## 22 · 剩下的问题：哪些是行业硬骨头，哪些是之后能动的

[22/25] 39s · 剩下的问题分两类

剩下的问题分两类。左边这些短期解决不了：大模型自身的判断质量、资产库只有两千零四十三个模型、还有一张图定不出真实尺寸这个根本困难。

尺寸这条我讲个具体机制。一个物体在图里只剩几厘米碎片时，程序算不出缩放比例，就退回 1。这个 1 看着中立，其实它在断言这东西恰好就是库里标的那么大；一查库，它就变成一个一米九高的书架。

右边那四条是下周就能动手的。

— 只给自己看 —
· 把「暂时无解」和「还没做」分开，是这一页存在的全部意义。

## 23 · 如果继续做下去，我认为真正的方向是什么

[23/25] 23s · 如果继续做下去

如果继续做，我觉得最有迁移价值的不是某个模块，而是一种做法：模型输出的是提议，不是结论。

近期把检查项变成可以求导的；中期接回美术的工具链，前提是先能把完整场景交出去；远期这套结构可以搬到 agent 上。

— 只给自己看 —
· 中期那张卡写了一个已知缺口：导出的 glb 位置和资产引用都对，但由多个部件组成的资产目前只装配了第一个部件，所以它适合看与验证、还不适合当成品。
· 被问到交付格式时直接用这句回答，不要含糊地说「可以」。

## 24 · 这两个月留下的，不只是一个更高的分数

[24/25] 8s · 留下的不只是分数

研究、工程、产品这三条线是同时闭环的，这也是我自己最看重的地方。

— 只给自己看 —
· 与自评页有重叠，不要重复讲成绩。

## 25 · 谢    谢

[25/25] 19s · 致谢

最后。两个月前，我只会说这个场景看起来不太对。现在我能说清它错在哪一项、差多少、为什么，以及凭什么敢改。

谢谢 Calvin——谢谢你在我想走捷径的时候，一直问我怎么证明。

— 只给自己看 —
· 慢下来，看着人说。说完停住，进入提问。

---

逐页秒数合计 **797 秒 ≈ 13.3 分钟**。
现场要压时间时，按这个顺序砍：第 19 页（已标 0 秒）→ 第 24 页 → 第 18 页 → 第 7 页 → 第 3 页只念四个词。**第 6 页与第 10–14 页不要砍。**
